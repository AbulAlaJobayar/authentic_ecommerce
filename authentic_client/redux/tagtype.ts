export enum tagType {
  auth = "auth",
  category = "category",
  Warehouse = "warehouse",
  suppliers = "suppliers",
  Product = "product",
  Inventory = "inventory",
  batch = "batch",
  discount="discount"
}
export const tagTypeList = [
  tagType.auth,
  tagType.category,
  tagType.Warehouse,
  tagType.suppliers,
  tagType.Product,
  tagType.Inventory,
  tagType.batch,
  tagType.discount
];
